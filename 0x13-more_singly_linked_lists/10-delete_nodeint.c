#include "lists.h"
/**
 * delete_nodeint_at_index - A function to delete node at specific index;
 * @head: the head pointer;
 * @index: the number of node to be deleted;
 * Return: 1 if (sccess) -1 if (fail);
 */
int delete_nodeint_at_index(listint_t **head, unsigned int index)
{
}
